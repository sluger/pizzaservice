import { ExtraNamePipe } from './extra-name.pipe';

describe('ExtraNamePipe', () => {
  it('create an instance', () => {
    const pipe = new ExtraNamePipe();
    expect(pipe).toBeTruthy();
  });
});
