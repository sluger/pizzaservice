import { IncredientsPipe } from './incredients.pipe';

describe('IncredientsPipe', () => {
  it('create an instance', () => {
    const pipe = new IncredientsPipe();
    expect(pipe).toBeTruthy();
  });
});
